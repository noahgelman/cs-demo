<header>
  <h1>CS Demo</h1>
</header>

<main role="main">
  <p>Content</p>
  {{#each @model as |file|}}
    <div>{{file.name}}</div>
    <div>{{file.device}}</div>
    <div>{{file.path}}</div>
    <div>{{file.status}}</div>
  {{/each}}
</main>